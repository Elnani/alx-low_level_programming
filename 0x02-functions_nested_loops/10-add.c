#include "main.h"
/**
 * add - addition of two int.
 * @m: variable to be considered.
 * @n:variable to be considered.
 * Return: sum (0).
 */
int add(int m, int n)
{
	int v;

	v = m + n;
	return (v);
}

