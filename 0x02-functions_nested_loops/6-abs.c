#include "main.h"
/**
 * _abs - returning an absolute integer
 * @c: variable containment
 * Return: Success (0)
 */
int _abs(int c)
{

	if (c < 0)
	{
		return (c * (-1));
	}
	else if (c > 0)
	{
		return (c);
	}
	else
	{
		return (c);
	}
}
