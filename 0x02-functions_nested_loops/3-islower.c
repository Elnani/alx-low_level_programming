#include "main.h"
/**
 * _islower - checking for small alphabets
 * @c: the character to be checked
 * Return: Success 1
 */

int _islower(int c)
{

	if (_islower(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
